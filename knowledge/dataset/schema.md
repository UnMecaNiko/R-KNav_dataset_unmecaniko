# Schema (sample, LeRobot v3)

Source: `meta/info.json` of the sample, read on 2026-08-23. If the Hub changes, the remote file wins.

- `codebase_version`: v3.0  
- `robot_type`: rover  
- `fps`: 10  
- `total_episodes`: 14  
- `total_frames`: 18054  
- `total_tasks`: 13  
- Video: H.264, 360×640, 10 fps, no audio  

## Features

| Name | Type | Shape | Notes |
|---|---|---|---|
| `observation.image.main` | video | 360×640×3 | Front camera (sync master) |
| `observation.image.left` | video | 360×640×3 | |
| `observation.image.right` | video | 360×640×3 | |
| `observation.image.rear` | video | 360×640×3 | |
| `observation.state.waypoints` | float32 | [2] | Card says (lon, lat). Sample Parquet inspection on 2026-09-06 found US-plausible extents: longitude `[-118.417488, -68.669060]`, latitude `[30.209423, 47.921959]`. No formal CRS tag is present. |
| `observation.state` | float32 | [2] | `twist.twist.linear.x`, `twist.twist.angular.z` — measured odometry |
| `action` | float32 | [2] | `twist.linear.x`, `twist.angular.z` — command (teleop or Nav2) |
| `observation.state.road_type` | string | [1] | e.g. sidewalk |
| `observation.state.surface` | string | [1] | e.g. asphalt |
| `observation.state.weather` | string | [1] | e.g. sunny |
| `observation.state.time_of_day` | string | [1] | e.g. daytime |
| `timestamp`, `frame_index`, `episode_index`, `index`, `task_index` | indices | | |
| Task (instruction) | string | | Generated by a VLM; examples in the Hub README |

There is no teleop/autonomous mode column. The README mentions an aligned IMU; it does not show up as a separate feature in the sample's `info.json` — confirm in the parquet.

`action` ≠ `observation.state`. The former is what was commanded; the latter, what was measured. Integrate them separately (odom vs GPS experiment).
